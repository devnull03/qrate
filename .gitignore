# rust
/target

# Packaging output, plus the runtime binaries scripts/fetch-binaries.sh drops beside a build.
/dist
pdfium.dll
libpdfium.so
libpdfium.dylib
ffmpeg
ffmpeg.exe

# AI stuff
.cursor/
.claude/
GEMINI.md
*handoff.md

# The Islandora plugin is its own repository, checked out here so a dev build loads it.
plugins/islandora/
